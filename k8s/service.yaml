apiVersion: v1
kind: Service
metadata:
  name: sentinel-mcp
  namespace: sentinel-system
  labels:
    app: sentinel-mcp
spec:
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: sentinel-mcp
